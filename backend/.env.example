# Gmail API Configuration (when wiring real Gmail)
# For MCP approach: no env vars needed, uses Claude Code MCP connector
# For OAuth approach: add your credentials below

# GOOGLE_CLIENT_ID=your_client_id_here
# GOOGLE_CLIENT_SECRET=your_client_secret_here
# GOOGLE_REDIRECT_URI=http://localhost:8000/auth/callback

# Claude API (for task extraction fallback)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Server config
HOST=0.0.0.0
PORT=8000
